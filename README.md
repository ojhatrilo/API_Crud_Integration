# Employee Management REST API
📌 Project Overview
        This project is a basic REST API built to manage employee records within a company. It focuses on core CRUD operations, strict RESTful principles, and secure JWT authentication

🛠️ Tech Stack
      Language: Python 3.x
      Framework: Django REST Framework (DRF)
      Database: MySQL
      Authentication: SimpleJWT (JSON Web Tokens)

🚀 Key Features
        Full CRUD Logic: Create, List, Retrieve, Update, and Delete employees.
        Advanced Filtering: Filter employees by department and role.
        Pagination: Results are limited to 10 employees per page.
        Robust Validation: Ensures unique email addresses and non-empty name fields.
        Standardized Error Handling: Returns proper HTTP status codes (201, 204, 400, 404). 
